{
    "packageName": "MCP Rule Server",
    "author": "kingpanther13",
    "version": "4.4.4",
    "minimumHEVersion": "2.3.0",
    "dateReleased": "2026-09-26",
    "documentationLink": "https://github.com/kingpanther13/Hubitat-local-MCP-server/blob/main/README.md",
    "communityLink": "",
    "releaseNotes": "v4.4.4 - 2026-09-26\n- fix: refused addAction editor cleanup, modern-era check, setVariable numOp, patches trigger/action ops (#471)\n  - Rule Machine: a rejected action add (unknown source variable, filtered device attribute, bad math operand, bad runCommand parameter) no longer leaves the Rule Machine action editor open with stale fields — the next action is created clean, and an editor that could not be closed is reported instead of hidden.\n  - Rule Machine: setVariable / setLocalVariable now support `numOp: \"add number\"` with `value` to add to the variable instead of setting it; unsupported operations are rejected loudly instead of being silently converted to a plain set.\n  - Rule Machine: `patches` now accepts `removeTrigger`, `modifyTrigger`, and `modifyAction`, so a trigger can be retargeted (remove + add) in one call.\n  - MCP protocol: future protocol revisions newer than 2026-07-28 will be handled as modern-era instead of falling back to legacy behavior.\n\nv4.4.3 - 2026-09-25\n- fix: hub_report_issue reports the real hub model; make bug-report error retention opt-in (#468)\n  - Bug reports generated by the server now show the hub's real hardware model (e.g. \"C-8 Pro\") instead of an internal id.\n  - Keeping recent errors for bug reports is now an opt-in advanced setting (off by default), which removes an app-state write from every logged error.\n  - `hub_call_rule` with `action=actions` runs a rule's actions through Rule Machine's own Run Actions button, so it keeps working when the hub's load limiter is blocking RMUtils.\n\nv4.4.2 - 2026-09-25\n- fix: resolve verified issue 407 findings (#465)\n  - Improve paused-rule feedback and avoid rejecting redundant device authorization when inventory is temporarily incomplete.\n  - Keep recent server errors available for issue reports even after native logs roll over.\n  - Apply a safe minimum to enabled request budgets and clarify backup recovery advice.\n- fix: hub_get_info reports the real hardware model, not the internal hardwareID (#466) (#467)\n  - `hub_get_info` now reports the hub's real hardware model (e.g. \"C-7\", \"C-8 Pro\") instead of an internal id. The raw internal id remains available under a new `platformHardwareId` field.\n- feat: add protected-app controls for generic app mutations (#456)\n  - Choose Protected apps in the MCP server settings to prevent generic MCP tools from editing, disabling, deleting, or operating those installed apps.\n  - The MCP server protects its own instance by default after installation or upgrade. You can change the selection in the Hubitat UI; dedicated Developer Mode maintenance remains available.\n\nv4.4.1 - 2026-09-23\n- fix: keep producedBy/triggered provenance in device event history (#459)\n  - Device event history now shows what caused each event: the app, rule, or device that produced it (`producedBy`) and the apps it triggered (`triggered`), plus the event type. Filter by `command-on` / `command-off` to see which rule or app sent a command.\n\nv4.4.0 - 2026-09-23\n- feat: add hub-level Hub Mesh tools (hub_get_hub_mesh, hub_update_hub_mesh) (#438)\n  - New: Hub Mesh tools — read which peer hubs your hub is meshed with (`hub_get_hub_mesh`: enabled state, peer hubs, shared/linked devices and hub variables, sync interval, mode-following hub) and change hub-level Hub Mesh settings (`hub_update_hub_mesh`: enable/disable, full-sync interval, follow a peer hub's modes, store a peer hub's mesh token)",
    "apps": [
        {
            "id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
            "name": "MCP Rule Server",
            "namespace": "mcp",
            "location": "https://raw.githubusercontent.com/kingpanther13/Hubitat-local-MCP-server/main/hubitat-mcp-server.groovy",
            "required": true,
            "oauth": true,
            "primary": true
        },
        {
            "id": "b2c3d4e5-f6a7-8901-bcde-f23456789012",
            "name": "MCP Rule",
            "namespace": "mcp",
            "location": "https://raw.githubusercontent.com/kingpanther13/Hubitat-local-MCP-server/main/hubitat-mcp-rule.groovy",
            "required": true,
            "oauth": false,
            "primary": false
        }
    ],
    "drivers": [],
    "bundles": [
        {
            "id": "7e3c1a8f-2b94-4d6e-9a51-2095099012ab",
            "name": "MCP Rule Server Libraries",
            "namespace": "mcp",
            "location": "https://raw.githubusercontent.com/kingpanther13/Hubitat-local-MCP-server/bundle-artifacts/branches/main/mcp-libraries.zip",
            "required": true
        }
    ]
}
